package com.TotalWork.CompleteTraining.repositry;

import com.TotalWork.CompleteTraining.model.EmailCustom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmailCustomRepository extends JpaRepository<EmailCustom, Long> {
}
